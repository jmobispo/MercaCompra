# Core
fastapi==0.110.1
uvicorn[standard]==0.25.0
pydantic==2.12.5
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1
aiosqlite==0.20.0
asyncpg==0.29.0

# Auth (uses stdlib hmac+hashlib for JWT and scrypt for passwords)
# No cryptography package required — avoids system conflicts
# Optional: passlib[bcrypt]==1.7.4 for bcrypt in production with proper system setup
python-multipart==0.0.9

# HTTP client (Mercadona proxy)
httpx==0.27.0

# Environment
python-dotenv==1.0.1

# Optional: Claude integration (not required for app to run)
# anthropic==0.30.0
